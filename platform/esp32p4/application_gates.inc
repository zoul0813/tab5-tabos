/* Every private ABI gate must participate in native task quiescence. */
NATIVE_VOID_GATE(console_write, (const char* text), (text))
NATIVE_VOID_GATE(request_exit, (int exit_status), (exit_status))
NATIVE_GATE(int, console_read, (char* buffer, uint32_t capacity), (buffer, capacity))
NATIVE_GATE(int, console_clear, (void), ())
NATIVE_GATE(int, fs_getcwd, (char* buffer, uint32_t capacity), (buffer, capacity))
NATIVE_GATE(int, fs_chdir, (const char* path), (path))
NATIVE_GATE(int, fs_list, (const char* path, char* buffer, uint32_t capacity), (path, buffer, capacity))
NATIVE_GATE(int, exec, (const char* path, uint32_t argc, const char* const* argv), (path, argc, argv))
NATIVE_VOID_GATE(yield, (void), ())
NATIVE_VOID_GATE(console_write_raw, (const char* text), (text))
NATIVE_GATE(int, fd_open, (const char* path, int flags, uint32_t mode), (path, flags, mode))
NATIVE_GATE(int, fd_close, (int descriptor), (descriptor))
NATIVE_GATE(int, fd_read, (int descriptor, void* buffer, uint32_t count), (descriptor, buffer, count))
NATIVE_GATE(int, fd_write, (int descriptor, const void* buffer, uint32_t count), (descriptor, buffer, count))
NATIVE_GATE(int, fd_seek, (int descriptor, int32_t offset, int whence, int32_t* position),
            (descriptor, offset, whence, position))
NATIVE_GATE(int, fs_stat, (const char* path, tabos_elf_stat_t* status), (path, status))
NATIVE_GATE(int, fd_stat, (int descriptor, tabos_elf_stat_t* status), (descriptor, status))
NATIVE_GATE(int, fs_mkdir, (const char* path, uint32_t mode), (path, mode))
NATIVE_GATE(int, fs_unlink, (const char* path), (path))
NATIVE_GATE(int, fs_rename, (const char* old_path, const char* new_path), (old_path, new_path))
NATIVE_GATE(int, fd_get_flags, (int descriptor), (descriptor))
NATIVE_GATE(int, fd_set_flags, (int descriptor, int flags), (descriptor, flags))
NATIVE_GATE(void*, heap_sbrk, (int32_t increment), (increment))
NATIVE_GATE(int, fs_rmdir, (const char* path), (path))
NATIVE_GATE(uint64_t, monotonic_ms, (void), ())
NATIVE_GATE(int, system_info, (tabos_elf_system_info_t * info), (info))
NATIVE_GATE(int, graphics_open, (uint32_t* width, uint32_t* height), (width, height))
NATIVE_GATE(int, graphics_clear, (uint32_t color), (color))
NATIVE_GATE(int, graphics_fill_rect, (int32_t x, int32_t y, uint32_t width, uint32_t height, uint32_t color),
            (x, y, width, height, color))
NATIVE_GATE(int, graphics_blit, (int32_t x, int32_t y, uint32_t width, uint32_t height, const uint16_t* pixels),
            (x, y, width, height, pixels))
NATIVE_GATE(int, graphics_present, (void), ())
NATIVE_GATE(int, graphics_close, (void), ())
NATIVE_GATE(uint32_t, graphics_capabilities, (void), ())
NATIVE_GATE(int, graphics_blit_ex, (const tabos_graphics_blit_options_t* options), (options))
NATIVE_GATE(int, tty_get_mode, (int descriptor), (descriptor))
NATIVE_GATE(int, tty_set_mode, (int descriptor, uint32_t mode), (descriptor, mode))
NATIVE_GATE(int, input_poll, (tabos_input_event_t * event), (event))
NATIVE_GATE(int, wall_time_get, (tabos_elf_wall_time_t * time), (time))
NATIVE_GATE(int, wall_time_set, (const tabos_elf_wall_time_t* time), (time))
NATIVE_GATE(int, system_action, (uint32_t action), (action))
NATIVE_GATE(int, network_status, (tabos_elf_network_status_t * status), (status))
NATIVE_GATE(int, network_connect_saved, (void), ())
NATIVE_GATE(int, network_disconnect, (void), ())
NATIVE_GATE(int, network_resolve, (const char* hostname, uint32_t family, tabos_elf_network_address_t* address),
            (hostname, family, address))
NATIVE_GATE(int, network_echo,
            (const tabos_elf_network_address_t* address, uint32_t sequence, uint32_t payload_bytes, uint32_t timeout_ms,
             tabos_elf_network_echo_result_t* result),
            (address, sequence, payload_bytes, timeout_ms, result))
NATIVE_GATE(int, socket_open, (uint32_t family, uint32_t type), (family, type))
NATIVE_GATE(int, socket_close, (int socket), (socket))
NATIVE_GATE(int, socket_bind, (int socket, const tabos_elf_socket_endpoint_t* endpoint), (socket, endpoint))
NATIVE_GATE(int, socket_listen, (int socket, uint32_t backlog), (socket, backlog))
NATIVE_GATE(int, socket_accept, (int socket, tabos_elf_socket_endpoint_t* peer), (socket, peer))
NATIVE_GATE(int, socket_connect, (int socket, const tabos_elf_socket_endpoint_t* endpoint), (socket, endpoint))
NATIVE_GATE(int, socket_set_nonblocking, (int socket, uint32_t enabled), (socket, enabled))
NATIVE_GATE(int, socket_shutdown, (int socket, uint32_t direction), (socket, direction))
NATIVE_GATE(int, socket_send, (int socket, const void* data, uint32_t size), (socket, data, size))
NATIVE_GATE(int, socket_receive, (int socket, void* data, uint32_t capacity), (socket, data, capacity))
NATIVE_GATE(int, socket_send_to,
            (int socket, const void* data, uint32_t size, const tabos_elf_socket_endpoint_t* endpoint),
            (socket, data, size, endpoint))
NATIVE_GATE(int, socket_receive_from, (int socket, void* data, uint32_t capacity, tabos_elf_socket_endpoint_t* peer),
            (socket, data, capacity, peer))
NATIVE_GATE(int, socket_get_local_endpoint, (int socket, tabos_elf_socket_endpoint_t* endpoint), (socket, endpoint))
NATIVE_GATE(int, battery_status, (tabos_elf_battery_status_t * status), (status))
NATIVE_GATE(int, battery_set_charging, (uint32_t enabled), (enabled))
NATIVE_GATE(int, battery_set_fast_charging, (uint32_t enabled), (enabled))
NATIVE_GATE(int, graphics_set_overlays, (uint32_t flags), (flags))
NATIVE_GATE(int, tls_connect, (const char* hostname, uint32_t port), (hostname, port))
NATIVE_GATE(int, tls_close, (int connection), (connection))
NATIVE_GATE(int, tls_send, (int connection, const void* data, uint32_t size), (connection, data, size))
NATIVE_GATE(int, tls_receive, (int connection, void* data, uint32_t capacity), (connection, data, capacity))
NATIVE_GATE(uint32_t, device_count, (void), ())
NATIVE_GATE(int, device_at, (uint32_t index, tabos_device_info_t* info), (index, info))
NATIVE_GATE(int, device_get, (tabos_device_id_t id, tabos_device_info_t* info), (id, info))
NATIVE_GATE(int, device_find, (const char* name, tabos_device_info_t* info), (name, info))
NATIVE_GATE(int, device_subscribe, (void), ())
NATIVE_GATE(int, device_subscription_close, (int subscription), (subscription))
NATIVE_GATE(int, device_event_read, (int subscription, tabos_device_event_t* event), (subscription, event))
NATIVE_GATE(int, socket_wait_source, (int socket), (socket))
NATIVE_GATE(int, device_subscription_wait_source, (int subscription), (subscription))
NATIVE_GATE(int, wait, (tabos_elf_wait_item_t * items, uint32_t count, uint32_t timeout_ms), (items, count, timeout_ms))
NATIVE_GATE(int, audio_info, (tabos_audio_info_t * info), (info))
NATIVE_GATE(int, audio_open, (const tabos_audio_config_t* config), (config))
NATIVE_GATE(int, audio_close, (int stream), (stream))
NATIVE_GATE(int, audio_write, (int stream, const void* pcm, uint32_t bytes), (stream, pcm, bytes))
NATIVE_GATE(int, audio_read, (int stream, void* pcm, uint32_t capacity), (stream, pcm, capacity))
NATIVE_GATE(int, audio_set_volume, (int stream, uint32_t volume), (stream, volume))
NATIVE_GATE(int, audio_set_route, (int stream, uint32_t route), (stream, route))
NATIVE_GATE(int, audio_status, (int stream, tabos_audio_status_t* status), (stream, status))
NATIVE_GATE(int, audio_wait_source, (int stream), (stream))
NATIVE_GATE(int, audio_flush, (int stream), (stream))
NATIVE_GATE(int, pointer_open, (tabos_device_id_t device_id), (device_id))
NATIVE_GATE(int, pointer_close, (int stream), (stream))
NATIVE_GATE(int, pointer_read, (int stream, tabos_pointer_event_t* event), (stream, event))
NATIVE_GATE(int, pointer_wait_source, (int stream), (stream))
NATIVE_GATE(int, camera_info, (tabos_device_id_t device_id, tabos_camera_info_t* info), (device_id, info))
NATIVE_GATE(int, camera_open, (const tabos_camera_config_t* config), (config))
NATIVE_GATE(int, camera_close, (int stream), (stream))
NATIVE_GATE(int, camera_acquire, (int stream, tabos_camera_frame_t* frame), (stream, frame))
NATIVE_GATE(int, camera_copy,
            (int stream, tabos_camera_lease_t lease, uint32_t offset, void* buffer, uint32_t capacity),
            (stream, lease, offset, buffer, capacity))
NATIVE_GATE(int, camera_release, (int stream, tabos_camera_lease_t lease), (stream, lease))
NATIVE_GATE(int, camera_wait_source, (int stream), (stream))
