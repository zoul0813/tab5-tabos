# Lua CLI validation — 2026-09-12

CLI software implementation is validated on host. Physical Tab5 acceptance remains
pending; graphics/input/audio/process bindings are not started. Linux builds/tests
were explicitly excluded by the user for this implementation.

## Build and provenance

- Official Lua 5.5.1 source archive SHA-256 verified against upstream; no reported
  upstream bugs at import. Provenance and six maintained vendor-file patches
  are listed in [UPSTREAM.md](../../apps/lua/UPSTREAM.md).
- Official 5.5.1 test archive SHA-256 verified; UTF-8/string/math files imported
  unchanged. Other suites are not claimed to pass.
- `./apps/build.sh` completed and installed the full normal application set. Lua,
  all three examples, license and provenance were byte-compared with their sources.
- RV32I/ilp32 application uses trailing `-lm`, standard 64-bit Lua integers and
  double precision. No unsupported fcntl link dependency remains after removing
  source-loader binary reopening. The loader's existing RWX-segment warning remains.
- Final `lua` ELF file: 741008 bytes; loaded text/data/BSS total: 367280 bytes.
- ELF SHA-256: `cdb5ddc78b18d53e5e0d6bc4605843b14b42396011f2195e8293d8bc1fd4c8da`.
- Metadata: ABI 3, heap 4194304 bytes, stack 65536 bytes, existing capability value 1.
- Build dependency manifest includes vendor headers and local configuration.
  `build.application_tracking` covers header/generated/Makefile/resource settings and
  changed/unchanged trailing application libraries. Normal builds require no download.
- macOS Debug/Release and ESP-IDF v5.4.4 Tab5 Debug/Release builds succeeded via
  `./tools/tabos <target> <configuration> build`.

## Automated results

macOS Release full CTest: **74/74 pass**. macOS Debug's full run initially found
one upstream `tab_funcs` naming-lint false positive; the narrow vendor exclusion and
all affected Lua/CLI/architecture checks passed on focused reruns. All 74 registered
checks have passing evidence. Parser/pattern-depth assertions also passed in both
configurations after their addition. Debug native application tests use ASan/UBSan.

Coverage includes:

- 349 allocation-failure thresholds during state/library setup, clean teardown,
  checked growth/shrink/free accounting, quota exhaustion and subsequent recovery.
- Cooked console edits, EOF/cancellation, bounded line overflow, buffered typeahead,
  Ctrl-C through a full queue and original TTY mode restoration.
- 64-bit integer wrap, floating math, closures/metatables, source loads, binary
  rejection, NUL paths, unsupported APIs, parser/pattern recursion and coroutine hooks.
- File bytes including NUL/255, flush/seek/close/rename/remove and missing-file errors;
  pure-Lua module loading/caching and invalid search paths.
- UTC leap dates, range errors, date formats, strict duration conversion and copied info.
- CLI action ordering, module aliases, script arguments/varargs, dash-prefixed files,
  ignored host environment/TZ, errors and os.exit statuses/finalizer reentry prevention.
- Official UTF-8, strings and math test subsets with portable/low-resource flags.

Actual RV32 integration passed using:

```sh
build/macos-debug/tests/tabos_lua_rv32 build/apps/shell/shell build/apps/lua/lua
```

This launches the real shell and same Lua artifact used by Tab5. It asserts version
and arithmetic stdout, script/module/argument behavior, host-drive binary bytes,
UTC through year 9999, OOM/recursion failure, error status, os.exit file finalization,
REPL expression/multiline/state recovery, Ctrl-C in loops and coroutines, shared
console reads/cancellation, idle guest suspension, parent restoration, forced runtime
teardown and fresh repeated launch. Temporary fixtures are removed on success.

## Remaining physical acceptance

No Lua firmware flash or physical test is claimed here. Follow [Lua](../lua.md) for
Aa/Sym typing, microSD files/modules, repeated launches, Ctrl-C latency, native
stack/heap high-water, service responsiveness and representative performance checks.
The proposed memory and hook budgets are not yet hardware measurements. Native faults
remain device-fatal under the existing Tab5 architecture.
