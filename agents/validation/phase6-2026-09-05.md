# Phase 6 Validation Evidence — 2026-09-05

Session paused by user on 2026-09-05. Preview and user-triggered snapshot work and
represent the expected application workflow. Automatic CLI still timing is deferred;
image-quality limitations and intermittent IO-expander failures remain recorded,
not resolved. No further Phase 6 testing or investigation until requested.

Historical session record. For current status use [the Phase 6 checklist](../hardware-services.md#phase-6-camera-and-media-pipeline). Paths under `build/` refer to local evidence, not committed artifacts.


Final cleanup run: operator reports 31 assertions, zero failures. Serial confirms all
12 RAW8 starts/stops complete (six child leaks and six parent reopens), with no
capture-time I2C/SCCB errors, CCM rejection, stack overflow, watchdog, or stalls.
Parent PSRAM snapshot returns to 15,722,100 bytes on each reopen; child snapshot is
15,304,284 bytes. Internal free memory settles at 275,719 parent / 267,187 child bytes
for the final three cycles. Earlier reset records precede tester launch/USB transfer.
Evidence: `build/phase6-camera-cleanup-pass.txt`. This closes the repeat-cycle
acceptance check; it does not guarantee absence of intermittent faults under every
possible condition. Saved RAW/RGB image inspection and JPEG timing remain pending.

Historical progression: pending/failure statements below describe their point in the
session and may be superseded by later entries. Use the Phase 6 checklist above for
current completion status.

Latest still inspection: supplied `camera.pgm` (921,616 bytes including 16-byte
header), `camera.rgb` (1,843,200 bytes, RGB565 little-endian), and `camera.jpg`
(22,069 bytes, three-component JPEG) all decode at 1280x720 but show nearly uniform
dark gray/noise, not the operator facing the camera. This fails still-image acceptance;
PGM's intended grayscale does not explain the missing scene in all three formats.
Original evidence preserved in `build/phase6-unsettled-stills/`; decoded inspection
PNGs are `build/phase6-{raw,rgb,jpeg}-view.png`.

Serial (`build/phase6-unsettled-stills-serial.txt`) records RAW8: two frames,
5,096,904 us interval, 4,268,850 us conversion; RGB565: eight frames, 9,374,870 us;
JPEG: two frames, 424,890 us, 157,903 us encoding. JPEG start free memory:
274,187 internal / 10,937,124 PSRAM bytes. No capture-time CCM/I2C errors logged.
Still utility previously saved the first acquired frame; later frames produced while
writing do not replace it. Working diagnosis is startup exposure/white-balance settling,
consistent with working longer previews. Updated utility acquires/releases startup
frames until at least six arrivals and two seconds of capture time, with bounded
failure and cooperative yields. H.264 path is unchanged. Physical diagnosis/fix
confirmation remains pending; do not mark image acceptance from valid byte counts.

Updated real RV32 `cameratest` passes all three still commands in the Debug host
runtime with ASan/UBSan libraries: RAW8 2.647 s, RGB565 2.237 s, JPEG 2.224 s;
all exit zero and write nonempty host-fixture outputs. Evidence:
`build/phase6-settle-host-{raw,rgb,jpeg}.log`. Host fixture dimensions are smaller
than Tab5; this verifies utility execution and settling delay, not physical exposure.

Physical settling rerun failed visual acceptance: operator still sees gray files,
while confirming live preview clearly shows a moving face/room. Updated utility
(74,321 loaded bytes) produced RAW8 11 backend frames / 7,039,759 us, RGB565 37 /
11,386,065 us, JPEG 58 / 2,360,320 us. Three IO-expander input-register failures
occurred during RAW8; no CCM rejection was logged. Evidence:
`build/phase6-settled-stills-serial.txt` and `build/phase6-settled-stills/`.
JPEG changed to 19,477 bytes and still visibly lacks scene detail; RGB hash also
changed, but PGM is byte-identical to the earlier sample, so that file does not
independently prove a new RAW8 image was transferred. Current H.264 first frame is
gray; frame 30 clearly shows a hand. A fixed startup delay is not an established fix.

Added `cameratest snapshot PATH`: S presents and saves the same leased RGB565 frame
using the existing chunked writer; Q/Escape cancels. This allows direct comparison
with the known-working preview before further capture/ISP changes. Current binary
loads 74,993 bytes. Actual RV32 host save and cancel paths both exit zero under
ASan/UBSan; save writes the complete 6,144-byte host frame, cancel creates no file.
Evidence: `build/phase6-snapshot-host-{save,cancel}.log`. Physical comparison pending.

Physical snapshot comparison: `snap.rgb` is 1,843,200 bytes, SHA-256
`8a4afcb4eda2b71d5dcc209fad81e88536e8386914547a351519181082c0d9da`.
Decoded `build/phase6-snapshot.png` shows a recognizable face and room with heavy
noise and blue/green tint. Operator confirms it matches preview and orientation is
correct. The shared leased-frame/chunked-write path can preserve a real scene;
automatic still acquisition remains unresolved. Serial records 108 backend frames
in 24,655,992 us, 27 dequeue misses, CCM correction applied without rejection, and
seven IO-expander input-register failures. Evidence: `build/phase6-snapshot-serial.txt`.
These failures occur during capture, unlike USB-transfer restart records surrounding
it. They recur in RGB565 as well as RAW8. Source inspection identifies the platform
headphone-detect poll as an IO-expander input reader; bus failure cause remains open.

- [x] Extend deterministic pool tests with exact drop counts, all-leased exhaustion,
  leased-byte preservation, slot reuse, copy bounds, foreign cleanup, and unavailable hardware.
- [x] Serialize runtime/wait capture updates with stream start/stop and add a threaded
  regression covering simultaneous update callers and repeated close/owner cleanup.
- [x] Run the expanded maintained tester on host and physical Tab5. The tester now uses
  native 1280x720@30 where supported instead of requesting unsupported 64x48 Tab5 capture.
- [x] Revalidate physical capture after the synchronization fix. The initial connected
  Tab5 session produced SCCB/IO-expander I2C failures, ISP CCM range errors, and a
  requeue-after-close `EBADF`; these are failed validation evidence, not acceptance.
- [x] Record RAW8/RGB565/H.264 performance and memory using serial stage counters;
  JPEG timing remains in the current remaining checklist.

macOS Debug and Release: all 46 tests pass in each configuration, including the threaded camera regression. Socket
component validation requires loopback access outside the filesystem sandbox.

Tab5 Debug and Release with synchronization fix and serial counters build successfully.
Pre-fix serial evidence is saved locally in `build/phase6-before-fix-serial.txt`.

The updated Debug firmware was flashed successfully (esptool hash verification).
A fresh serial session is waiting for post-fix physical commands.

Post-fix physical RAW8 run: 1280x720@30 configured; 2 backend frames over
5,046,760 us, 397,711 us total conversion (198,855.5 us/frame), no encoding.
Allocation snapshot: 274,959 internal free bytes and 12,781,160 PSRAM free bytes.
No requeue-after-close error appeared in this run, but repeated SCCB and IO-expander
I2C failures persisted. This is not a throughput/coexistence pass. Operator confirmed `cameratest capture raw camera.pgm` succeeded and reported
921,600 payload bytes, matching 1280x720 RAW8. Decoded image contents, drop count,
and input responsiveness still await validation.

- [x] Physically capture RAW8 and write its 921,600-byte payload to microSD
  through `cameratest capture raw camera.pgm` (operator confirmation).

RGB565 capture attempt returned status 1 without a screen diagnostic. Serial shows
application load but no capture start. Installed utility freshness is unverified;
replace the SD utility with the current build before attributing this to hardware.

- [x] Physical RGB565 capture writes 1,843,200 bytes after replacing the SD utility
  with the current binary (operator confirmation).
- [ ] Investigate the operator-observed brief blue screen during RGB565 still capture.
  The corresponding serial run reports 2 frames over 9,577,317 us, repeated ISP CCM
  range errors, and a task-watchdog report with `tabos-app` in SDMMC event waiting.
  Successful file output does not establish display/storage responsiveness.

### Follow-up fixes awaiting physical rerun

- Bundled SC202CS 2292 K CCM contains 4.5445, exceeding the ISP coefficient limit.
  Board-owned custom tuning blends this matrix 84% toward the original from identity;
  its maximum is 3.97738 and row sums remain 1. Automatic exposure/white balance stay enabled.
- Camera utility now copies 4 KiB at a time and yields every 64 KiB to mitigate
  SD-write starvation. It closes files on write failure and diagnoses unknown capture formats.
- Both firmware and SD `cameratest` must be updated before validating these changes.
- I2C errors and the brief blue screen remain unresolved pending rerun and diagnosis.

Follow-up build validation: camera utility and custom-tuning Tab5 Debug firmware
build successfully. All CCM table coefficients and neutral row sums pass a JSON
check; only the 2292 K matrix differs from the upstream tuning. Build dependencies
confirm the firmware embeds the board-owned `sc202cs.json`.

Custom-tuning Debug firmware flashed successfully with esptool hash verification.
Awaiting updated SD utility and physical RGB capture rerun.

Post-yield/custom-tuning RGB565 rerun: operator reports 1,843,200 bytes written
and no blue flash. No task-watchdog report occurred between capture start and stop.
Backend processed 8 frames over 10,188,907 us; allocation snapshot was 276,011
internal free bytes and 12,781,160 PSRAM free bytes. Two IO-expander read errors
and repeated CCM coefficient 4.328273 errors remain. Generated IPA configuration
contains the adjusted 3.9774 table coefficient, so bounding input table values alone
does not bound algorithm output. The earlier claim that the ISP fault was fixed
was premature; runtime CCM validation remains open. Serial evidence is saved in
`build/phase6-tuning-rgb-serial.txt`.

H.264 physical recording: operator reports 30 frames written and 16 dropped for
`cameratest record h264 camera.h264 30`. Backend reports 49 frames over
31,949,071 us and 6,111,496 us total encoding (124,724.4 us/backend frame).
The 30-frame file took approximately 31.95 seconds to record; configured 30 FPS
is not achieved. Runtime CCM range errors persist. Decoding, picture contents,
and dropped-reference-frame effects remain unverified; output success alone is
not an H.264 acceptance pass.

H.264 decode validation of operator-provided `camera.h264` (6,160,664 bytes):
FFprobe identifies 1280x720 H.264 but returns only 15 decoded pictures. Annex-B
contains one SPS, one PPS, one IDR slice, and 29 non-IDR slices. Slice frame numbers
have gaps and a later reset without a retained IDR, consistent with dropping encoded
reference pictures from the frame pool. Strict FFmpeg decoding exits zero, but this
does not establish that every written picture survived. Extracted first/middle/last
decoded pictures show a dark initial frame and strong cyan/noise in later images;
color accuracy remains unverified. Timing metadata does not provide a usable frame
rate (FFprobe reports 1200000/1); do not treat this as measured FPS.
Evidence: `build/phase6-h264-headers.log` and `build/phase6-h264-contact.png`.
H.264 acceptance remains failed: encoded reference dependencies need protection
from the generic drop policy, followed by a fresh recording/decode test.

H.264 reference-preservation fix: serialized service updates now defer backend capture
and encoding while the three-slot encoded pool is full. Releasing a lease permits the
next encoded picture; acquiring alone does not. RAW8/RGB565/JPEG retain their existing
drop policy. Unexpected encoded submissions to a full pool and oversized frames fault
the stream instead of silently discarding references or truncating frames. The platform
contract requires at most one synchronous frame callback per update. Upstream sensor
skips remain outside the pool-drop counter.

The threaded component regression verifies stalled ready and leased pools, consecutive
encoded payloads after release, zero pool drops, unexpected callback errors, and oversized
frame rejection. All 46 host tests pass in Debug and Release (loopback socket tests
require an unsandboxed rerun). Corrected Debug firmware built and flashed successfully.
Existing `cameratest` needs no replacement for this kernel fix.

Physical rerun passes reference-preservation validation: operator reports 30 written,
zero dropped. Replacement `camera.h264` SHA-256
`06a47371f98e5376c521579d629dbcca24de452b039e15614b48b498a91c673e`
decodes to all 30 pictures at 1280x720 with FFprobe `-count_frames`; FFmpeg
`-v warning -err_detect explode -i camera.h264 -f null -` exits zero without warnings.
Serial evidence in `build/phase6-h264-fixed-serial.txt`: 33 backend frames over
17,534,336 us, 1,736,175 us encoding total (52.6 ms/backend frame), internal free
171,195 bytes and PSRAM free 10,937,624 bytes at start. Written throughput is about
1.71 pictures/s. One transient dequeue warning (`errno=1`) occurs during capture;
no capture-time watchdog or CCM error appears in this run. A later watchdog reset
and USB mass-storage boot occur after capture stopped. Color accuracy, previously
observed runtime CCM/I2C issues, and broader Phase 6 coexistence remain unresolved.

Operator subsequently viewed corrected H.264 playback and reports video looks "ok".
Record this as basic visual acceptance, not calibrated color accuracy or separately
confirmed orientation. Maintained tester and concurrent-service validation remain pending.

Operator ran the updated `tester --camera` on Tab5: 33 camera assertions, zero
failures. Physical maintained camera API validation passes; host execution of this
expanded application and concurrent-service validation remain pending.

Operator reports physical preview looks "ok" and `q` exits correctly. Preview visual
acceptance and keyboard-driven exit pass. Normal shell typing after exit and broader
audio/network/storage coexistence are not yet separately confirmed.

Host maintained camera application validation now passes: a temporary headless runner
in `build/phase6-camera-host.c` starts the real runtime, launches installed RV32
`T:/bin/tester --camera`, and observes exit status zero. Linked Debug libraries use
ASan/UBSan; no sanitizer errors reported. Evidence: `build/phase6-camera-host.log`.
The logged process-0 exit panic is the expected root-exit policy, not a tester failure.
This checks the application camera assertions; shell launch/return and simultaneous
audio/network/storage activity require separate validation.

Operator subsequently ran full `tester` on Tab5 after preview: 176 assertions,
zero failures. Post-camera SDK regression passes. This is sequential service
validation; it does not establish simultaneous camera/audio/network progress.

Added opt-in `tester --camera-services` with six rounds of RGB565 capture, queued
silent stereo audio, nonblocking UDP loopback, and exclusive scratch-file write/readback.
Each round checks newer camera sequence, up to 4 KiB of leased frame data through
storage, exact UDP payload, audio queue drain within five seconds, and lease release.
All resources close and the owned scratch file is removed, including setup failures.
This measures overlapping service progress, not glitch-free sustained playback,
full-frame SD throughput, or Wi-Fi-radio traffic. Physical execution is pending.

The real RV32 tester passes in a temporary host runner using SDL dummy video/audio
and Debug ASan/UBSan libraries (exit zero, no sanitizer errors). The sandboxed run
fails when socket binding is blocked; an unsandboxed local-loopback run passes.
Runner: `build/phase6-camera-services-host.c`. No firmware change is needed for this
test; replace the SD-card tester binary.

CCM investigation: `esp_video_isp_device.c:isp_init_ccm_param()` multiplies matrix
red/blue columns by white-balance gains when `ESP_VIDEO_ISP_DEVICE_WBG` is undefined.
Current `CONFIG_ESP32P4_REV_MIN_FULL=1` selects this path; separate WBG requires revision
300 or newer. This explains why bounding the JSON matrix does not bound final driver
coefficients. Exact failing gain/matrix values still need instrumentation before a
color-preserving correction is chosen. No further tuning change made in this step.

First physical `tester --camera-services` run reset the device. Serial confirms
`A stack overflow in task tabos-app has been detected` immediately after RGB565
capture start. This is a defect in the new tester: three 4 KiB automatic buffers
consumed most of the 16 KiB native application stack before nested service calls.
Evidence: `build/phase6-camera-services-stack-overflow.txt`.

Moved all three buffers to checked, zero-initialized application heap storage, freed
on normal completion and early file-open failure. RV32 GCC `-Os -fstack-usage` reports
the test function's static frame falling from 12,688 bytes to 400 bytes. This figure
excludes callees. Tester rebuilt; physical rerun pending. The interrupted run can
leave `T:/tabos-camera-services.tmp`; remove that test artifact before rerunning,
because exclusive creation deliberately refuses an existing file.
The rebuilt RV32 application passes the host overlap runner with SDL dummy audio,
local UDP access, and ASan/UBSan libraries (exit zero, no sanitizer errors).

Corrected overlap test passes physically: operator reports 43 assertions, zero
failures, six rounds, elapsed 632 ms and longest round 155 ms. Serial records
RGB565 1280x720@30, seven backend frames over 689,649 us, internal free 275,127 bytes,
PSRAM free 15,689,316 bytes, and zero conversion/encoding time for direct RGB565.
Seven transient dequeue warnings (`errno=1`) occur during capture, but no capture-time
stack overflow, watchdog, CCM, or I2C errors. Earlier resets in this log precede tester
launch and USB-storage transfer. Evidence: `build/phase6-camera-services-pass.txt`.
This establishes bounded camera/audio/UDP-loopback/storage progress for this short
run. Wi-Fi-radio traffic, sustained audio quality, and prior CCM/I2C faults remain
outside this pass. Operator did not report the pool-drop summary value.

Dequeue investigation: pinned `esp_video_ioctl_dqbuf()` returns `ESP_FAIL` when
`esp_video_recv_element()` returns NULL after its ready-semaphore wait; VFS maps that
to `EPERM`. Camera config sets a 10 ms timeout. Backend now counts failed dequeues in
`dequeue_misses`, suppresses ordinary EAGAIN/ETIMEDOUT/EPERM per-poll warnings, and
warns once after two seconds without a successful dequeue. Other errors warn immediately.
EPERM is also possible for preprocessing failures, so it is not unconditionally
classified as harmless and the prolonged-stall diagnostic remains.

Added a linker wrapper for `esp_isp_ccm_configure` to log the first rejected final
matrix per boot, preserving input and result. No managed dependency files changed;
CCM correction remains pending physical evidence. Debug and Release Tab5 builds and
all four architecture checks pass; Debug firmware flashed. Next physical step is
varied-light preview with serial capture and simultaneous Mac-to-Tab5 Wi-Fi pings.

Wi-Fi baseline attempt before preview: Tab5 reports 192.168.86.87, but Mac ping
returns `No route to host` (28 transmitted, zero received). Stopped probes; this is
not evidence of camera starvation because preview had not begun. Further route
inspection required host permissions and was declined. Network reachability must
be established before Wi-Fi overlap validation. Diagnostic serial monitor remains
active for varied-light preview; no new application binary is required.

Operator preview/Wi-Fi run: preview behaves normally; 44 pings sent/received, zero
loss, RTT min/avg/max/stddev 19.913/142.931/1037.393/149.362 ms. This establishes Wi-Fi
progress, not a low-latency guarantee; no idle baseline exists for attributing the
one-second peak to camera activity. Serial records 153 RGB565 frames over 24,489,788 us
(6.25 backend frames/s), 57 dequeue misses, internal free 274,483 bytes and PSRAM free
12,781,160 bytes. No stall warning or capture-time reset. Evidence:
`build/phase6-preview-wifi-ccm.txt`.

CCM failure reproduced eight times. First rejected final matrix:
`[2.950245 -0.407000 -1.588085; -0.727611 1.953100 -0.613378; -0.340350 -1.331600 4.318523]`.
Added a platform-local bound after white-balance multiplication: interpolate towards
a diagonal matrix with identical row sums, selecting the largest common interpolation
factor fitting all coefficients within +/-3.99 (hardware limits +/-4). This retains
the neutral RGB response while reducing color correction. Already-valid matrices pass
unchanged; nonfinite values or unsupported diagonal baselines remain driver errors.
Caller/vendor matrix state is not modified. First correction and remaining rejection
are logged once per boot. Regression covers the measured failure, negative overflow,
50 neutral-preservation cases, identity/boundary stability, and unsupported inputs.
Physical correction and color acceptance remain pending.
Validation: all 47 host tests pass in Debug and Release (socket test rerun with
local-loopback permission), both Tab5 builds pass, and corrected Debug firmware
flashed successfully. Existing `cameratest` remains compatible.

Corrected firmware physical preview passes: operator reports everything works as
expected. Ping statistics: 48 sent/received, zero loss; RTT min/avg/max/stddev
23.014/134.414/351.821/71.205 ms. Serial confirms `CCM bounded after white balance;
neutral response preserved`, followed by clean capture stop: 241 backend RGB565
frames over 39,672,747 us (6.07 frames/s), 120 dequeue misses, internal free 273,303
bytes, PSRAM free 12,781,160 bytes. No CCM rejections, I2C errors, capture stalls,
watchdog, or reset appear during this run. Evidence: `build/phase6-preview-ccm-fixed.txt`.
This validates the correction was exercised and basic image quality remained
acceptable, plus Wi-Fi progress during preview. RTT improvement over the prior run
is observational; without matched conditions it is not proof of causation.
Earlier intermittent I2C failures are not proven resolved by this clean run.

Checklist reconciliation: the main Phase 6 section now marks individual completed
checks and lists only concrete remaining acceptance work. Added maintained
`tester --camera-cleanup`: six children each acquire a RAW8 frame and exit 89 with
stream/lease/wait source held; each parent iteration reopens, receives/copies a frame,
closes with its lease held, and verifies the closed wait source is stale. Successful
physical execution should report 31 assertions, zero failures, and exercise 12 starts/stops.
The rebuilt real RV32 tester passes on host under ASan/UBSan. Evidence:
`build/phase6-camera-cleanup-host.log`. Physical test and saved-image inspection remain.
