# Graphics Demo

Fullscreen TabOS graphics API demonstration. Run `graphics-demo`, move the square with
W/A/S/D, press R to rotate the scaled, color-keyed sprite, and press Q to exit. The
same extensionless RV32 executable runs on host and Tab5.
